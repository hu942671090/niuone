"""NiuOne application domains.

Executable modules live in :mod:`app.entrypoints`; historical bare-module
adapters live in :mod:`app.compat`. Reusable implementation code is organized
into domain packages such as ``dashboard``, ``messaging``, ``reports``,
``strategies``, and ``trading``.
"""
